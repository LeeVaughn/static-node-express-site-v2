extends layout

block content
  div.container.grid-container.portfolio-intro.portfolio-project
    h1= `404 - Page Not Found`
    p.not-found-p= `Oops!  It looks like the page you're looking for does not exist.`

    a.home(href=`/`) Home

    if err

      div.not-found-stack
        hr
        pre #{err.stack}
